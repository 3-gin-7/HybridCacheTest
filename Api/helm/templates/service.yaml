apiVersion: v1
kind: Service
metadata:
  name: {{ include "cache-api.fullname" . }}
  labels:
    {{- include "cache-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cache-api.selectorLabels" . | nindent 4 }}
