apiVersion: v1
kind: Service
metadata:
  name: {{ include "HybridCacheApi.fullname" . }}
  labels:
    {{- include "HybridCacheApi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "HybridCacheApi.selectorLabels" . | nindent 4 }}
